name: bcftools_env
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bcftools=1.13  # Or any specific version of bcftools you'd like
  - htslib         # Required for bcftools to work properly
  - zlib           # Compression library for handling gzipped files
